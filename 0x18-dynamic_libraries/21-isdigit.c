#include "main.h"

/**
 * _custom_isdigit - checks if a character is a digit
 * @ch: the character to be checked
 *
 * Return: 1 if @ch is a digit, 0 otherwise
 */
int _custom_isdigit(int ch)
{
    return (ch >= '0' && ch <= '9');
}
